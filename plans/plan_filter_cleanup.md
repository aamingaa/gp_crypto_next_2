# Plan: 筛选逻辑清理

## 目标
先移除“人工固定阈值”的硬性筛选影响：仅注释原有硬阈值逻辑，不引入新的 P90 交集筛选。

## 影响范围
- 文件：`main_gp_new.py`
- 函数：`read_and_pick()`
- 环节：`elite_factors_further_process()` 的 elite pool 构建

## 实施步骤（Checklist）
- [ ] 读取因子总表逻辑保持不变。
- [ ] 注释掉固定阈值判断（如 `sharp > 2`、`avg_pic > 0.005`）代码块。
- [ ] 临时采用“无硬门槛”或“最小过滤”策略（仅保留可解析表达式）。
- [ ] 加入缺失列/空样本/NaN 的容错。
- [ ] 输出生效模式（如 `no_hard_filter`）与结果数量。

## 验证
1. `read_and_pick()` 正常运行。
2. 固定阈值分支不再触发。
3. `elite_pool` 数量较原逻辑明显上升。
4. 小样本情况下不崩溃。

## 执行结果与验证记录
- 待执行
